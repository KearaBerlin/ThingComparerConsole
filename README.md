This is the console version of Thing Comparer. You can run the program from a terminal by
navigating to the directory containing ThingComparerConsole.class, then running
this command:

`java ThingComparerConsole`
